<Window x:Class="NetMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NetMonitor.ViewModels"
        mc:Ignorable="d"
        Title="ISM+ Dashboard" Height="700" Width="1000"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style TargetType="Button" x:Key="SidebarButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Padding" Value="10,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}" Margin="10,0">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Path" x:Key="SidebarIcon">
            <Setter Property="Fill" Value="LightGray"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="SidebarText">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="15,0,0,0"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsMouseOver, ElementName=SidebarBorder}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border x:Name="SidebarBorder" Grid.Column="0" Background="#111111" CornerRadius="0,15,15,0" Padding="0,20" HorizontalAlignment="Left">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Width" Value="70"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Width" Value="220"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            
            <StackPanel Orientation="Vertical">
                
                <Button Command="{Binding OpenDashboardCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SidebarButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentView}" Value="Dashboard">
                                    <Setter Property="Background" Value="#33FFFFFF"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <Path Style="{StaticResource SidebarIcon}" Fill="Orange" Data="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"/>
                        <TextBlock Style="{StaticResource SidebarText}" Text="Home"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding ShowAppMonitorCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SidebarButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentView}" Value="AppMonitor">
                                    <Setter Property="Background" Value="#33FFFFFF"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <Path Style="{StaticResource SidebarIcon}" Fill="LimeGreen" Data="M21 2H3c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h7v2H8v2h8v-2h-2v-2h7c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm0 14H3V4h18v12z"/>
                        <TextBlock Style="{StaticResource SidebarText}" Text="🔒 Monitor"/>
                    </StackPanel>
                </Button>
                
                <Button Command="{Binding ShowGraphCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SidebarButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentView}" Value="Graph">
                                    <Setter Property="Background" Value="#33FFFFFF"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <Path Style="{StaticResource SidebarIcon}" Fill="DodgerBlue" Data="M3.5 18.5l6-6 4 4L22 7M3.5 18.5V12l6-6 4 4 8.5-8.5"/>
                        <TextBlock Style="{StaticResource SidebarText}" Text="🔒 Graph"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding ShowSettingsCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource SidebarButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentView}" Value="Settings">
                                    <Setter Property="Background" Value="#33FFFFFF"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <Path Style="{StaticResource SidebarIcon}" Fill="Gray" Data="M19.43 12.98c.04-.32.07-.64.07-.98s-.03-.66-.07-.98l2.11-1.65c.19-.15.24-.42.12-.64l-2-3.46c-.12-.22-.39-.3-.61-.22l-2.49 1c-.52-.4-1.08-.73-1.69-.98l-.38-2.65C14.46 2.18 14.25 2 14 2h-4c-.25 0-.46.18-.49.42l-.38 2.65c-.61.25-1.17.59-1.69.98l-2.49-1c-.23-.09-.49 0-.61.22l-2 3.46c-.13.22-.07.49-.12-.64l2.11 1.65c-.04.32-.07.65-.07.98s.03.66.07.98l-2.11 1.65c-.19.15-.24.42-.12.64l2 3.46c.12.22.39.3.61.22l2.49-1c.52.4 1.08.73 1.69.98l.38 2.65c.03.24.24.42.49.42h4c.25 0 .46-.18.49-.42l.38-2.65c.61-.25 1.17-.59 1.69-.98l2.49 1c.23.09.49 0 .61-.22l2-3.46c.12-.22.07-.49-.12-.64l-2.11-1.65zM12 15.5c-1.93 0-3.5-1.57-3.5-3.5s1.57-3.5 3.5-3.5 3.5 1.57 3.5 3.5-1.57 3.5-3.5 3.5z"/>
                        <TextBlock Style="{StaticResource SidebarText}" Text="Settings"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="20">
            <Grid x:Name="DashboardView">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentView}" Value="Dashboard">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <Grid Grid.Row="0" Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="200" />
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Orientation="Vertical" Grid.Column="0" VerticalAlignment="Top">
                        <TextBlock Text="INTERFACE" Foreground="{DynamicResource TextBrush}" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding AvailableInterfaces}" SelectedItem="{Binding SelectedNetworkInterface}" Width="150" Height="25" HorizontalAlignment="Left"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="⚡" FontSize="38" FontWeight="Black" Foreground="DodgerBlue" Margin="0,0,10,0"/>
                            <TextBlock Text="ISM+" FontSize="42" FontWeight="Black" Foreground="Orange"/>
                        </StackPanel>
                        <TextBlock Text="KNOW THE LIMITS!" FontSize="18" FontWeight="Bold" Foreground="LimeGreen" HorizontalAlignment="Center" Margin="0,-5,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- TILE ROW 1 -->
                <Border Grid.Row="1" Background="{DynamicResource CardBrush}" CornerRadius="10" Padding="20" Margin="10,10,10,20" BorderBrush="Orange" BorderThickness="1">
                    <Grid>
                        <StackPanel Opacity="0.1">
                            <TextBlock Text="In-App Speed Test" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                            <TextBlock Text="Looking up ISP..." Foreground="Gray" FontSize="12" Margin="0,2,0,10" FontStyle="Italic"/>

                            <StackPanel Orientation="Horizontal">
                                <StackPanel Margin="0,0,30,0">
                                    <TextBlock Text="Ping: " Foreground="Gray" FontSize="14"/>
                                    <TextBlock Text="--" Foreground="LightGray" FontSize="18" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,30,0">
                                    <TextBlock Text="Download: " Foreground="Gray" FontSize="14"/>
                                    <TextBlock Text="--" Foreground="#FFA500" FontSize="18" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,30,0">
                                    <TextBlock Text="Upload: " Foreground="Gray" FontSize="14"/>
                                    <TextBlock Text="--" Foreground="#800080" FontSize="18" FontWeight="Bold"/>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                                <Button Content="Run Test" Background="#FF4500" Foreground="White" BorderThickness="0" Padding="10,8" Width="100" FontWeight="Bold"/>
                                <Button Content="Stop" Background="DarkRed" Foreground="White" BorderThickness="0" Padding="10,8" Width="80" Margin="10,0,0,0" FontWeight="Bold"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="🔒  In-App Speed Test is available in ISM+ Pro" FontSize="15" FontWeight="Bold" Foreground="Orange" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock HorizontalAlignment="Center" FontSize="13" Foreground="DodgerBlue" TextDecorations="Underline" Cursor="Hand" MouseLeftButtonDown="OpenProStoreLink">
                                Get ISM+ Pro on the Microsoft Store →
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- TILE ROW 2 -->
                <Grid Grid.Row="2" Margin="10,0,10,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{DynamicResource CardBrush}" CornerRadius="10" Padding="20" BorderBrush="Orange" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Current Speed" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                                <TextBlock Text="↓ " FontSize="22" FontWeight="Black" Foreground="{DynamicResource DownloadSpeedBrush}"/>
                                <TextBlock Text="{Binding CurrentDownloadSpeed}" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource DownloadSpeedBrush}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="↑ " FontSize="22" FontWeight="Black" Foreground="{DynamicResource UploadSpeedBrush}"/>
                                <TextBlock Text="{Binding CurrentUploadSpeed}" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource UploadSpeedBrush}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Background="{DynamicResource CardBrush}" CornerRadius="10" Padding="20" BorderBrush="Orange" BorderThickness="1">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Total Data Usage:" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                                <ComboBox ItemsSource="{Binding UsagePeriodValues}" SelectedItem="{Binding SelectedUsagePeriod}" Margin="10,0,0,0" Width="120" Height="25"/>
                            </StackPanel>
                            <TextBlock Text="{Binding DisplayedUsage}" FontSize="32" FontWeight="Black" Foreground="{DynamicResource DownloadUsageBrush}" Margin="0,15,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- TILE ROW 3 -->
                <Grid Grid.Row="3" Margin="10,0,10,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{DynamicResource CardBrush}" CornerRadius="10" Padding="20" BorderBrush="Orange" BorderThickness="1">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="Max Speeds:" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                                <ComboBox ItemsSource="{Binding MaxSpeedPeriodValues}" SelectedItem="{Binding SelectedMaxSpeedPeriod}" Margin="10,0,0,0" Width="120" Height="25"/>
                            </StackPanel>
                            
                            <Grid Margin="0,5,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Download" FontSize="12" Foreground="Gray" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding DisplayedMaxSpeed}" FontSize="22" FontWeight="Bold" Foreground="#FFB200" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Upload" FontSize="12" Foreground="Gray" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding DisplayedMaxUploadSpeed}" FontSize="22" FontWeight="Bold" Foreground="#800080" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Background="{DynamicResource CardBrush}" CornerRadius="10" Padding="20" BorderBrush="Orange" BorderThickness="1">
                        <Grid>
                            <StackPanel Opacity="0.1">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Avg. Speed:" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                                    <ComboBox Margin="10,0,0,0" Width="120" Height="25"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                                    <TextBlock Text="↓ " FontSize="18" FontWeight="Bold" Foreground="{DynamicResource DownloadSpeedBrush}"/>
                                    <TextBlock Text="--" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource DownloadSpeedBrush}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="↑ " FontSize="18" FontWeight="Bold" Foreground="{DynamicResource UploadSpeedBrush}"/>
                                    <TextBlock Text="--" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource UploadSpeedBrush}"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="🔒  Average Speed is available in ISM+ Pro" FontSize="14" FontWeight="Bold" Foreground="Orange" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock HorizontalAlignment="Center" FontSize="12" Foreground="DodgerBlue" TextDecorations="Underline" Cursor="Hand" MouseLeftButtonDown="OpenProStoreLink">
                                    Get ISM+ Pro →
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>

            <!-- APP MONITOR VIEW -->
            <Grid x:Name="AppMonitorView">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentView}" Value="AppMonitor">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Border Background="#11FF8C00" BorderBrush="Orange" BorderThickness="1" CornerRadius="10" Padding="40" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="🔒  App Bandwidth Tracker is available in ISM+ Pro" FontSize="20" FontWeight="Bold" Foreground="Orange" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                        <TextBlock Text="Monitor real-time network usage per application." FontSize="15" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                        <Border Background="DodgerBlue" CornerRadius="6" Padding="20,10" Cursor="Hand" HorizontalAlignment="Center" MouseLeftButtonDown="OpenProStoreLink">
                            <TextBlock Text="Get ISM+ Pro on the Microsoft Store →" Foreground="White" FontWeight="Bold" FontSize="14"/>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- GRAPH VIEW -->
            <Grid x:Name="GraphView">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentView}" Value="Graph">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Border Background="#11FF8C00" BorderBrush="Orange" BorderThickness="1" CornerRadius="10" Padding="40" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="🔒  Real-Time Graph is available in ISM+ Pro" FontSize="20" FontWeight="Bold" Foreground="Orange" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                        <TextBlock Text="Visualize your download and upload speeds over time." FontSize="15" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                        <Border Background="DodgerBlue" CornerRadius="6" Padding="20,10" Cursor="Hand" HorizontalAlignment="Center" MouseLeftButtonDown="OpenProStoreLink">
                            <TextBlock Text="Get ISM+ Pro on the Microsoft Store →" Foreground="White" FontWeight="Bold" FontSize="14"/>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- SETTINGS VIEW -->
            <Grid x:Name="SettingsView">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentView}" Value="Settings">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                
                <ScrollViewer VerticalScrollBarVisibility="Auto" PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="10" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Settings &amp; Preferences" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,15"/>
                            
                            <GroupBox Header="General Preferences" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,15" Padding="10">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Theme:" Width="150" VerticalAlignment="Center" FontSize="14" FontWeight="Normal"/>
                                        <ComboBox ItemsSource="{Binding ThemeValues}" SelectedItem="{Binding SelectedTheme}" Width="150"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Speed Unit:" Width="150" VerticalAlignment="Center" FontSize="14" FontWeight="Normal"/>
                                        <ComboBox ItemsSource="{Binding SpeedUnitValues}" SelectedItem="{Binding SelectedSpeedUnit}" Width="150"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Run at Startup:" Width="150" VerticalAlignment="Center" FontSize="14" FontWeight="Normal"/>
                                        <CheckBox IsChecked="{Binding RunAtStartup}" VerticalAlignment="Center">
                                            <CheckBox.LayoutTransform><ScaleTransform ScaleX="1.3" ScaleY="1.3"/></CheckBox.LayoutTransform>
                                        </CheckBox>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Scrolling Speed:" Width="150" VerticalAlignment="Center" FontSize="14" FontWeight="Normal"/>
                                        <Slider Minimum="0.1" Maximum="2.0" Value="{Binding Settings.ScrollSpeedMultiplier}" Width="150" VerticalAlignment="Center" TickFrequency="0.1" IsSnapToTickEnabled="True" IsMoveToPointEnabled="True"/>
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>

                            <GroupBox FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,15" Padding="10">
                                <GroupBox.Header>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🔒" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="Graph &amp; Chart Options" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        <Border Background="#CCFF8C00" CornerRadius="4" Padding="6,2" Margin="10,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="PRO" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                        </Border>
                                    </StackPanel>
                                </GroupBox.Header>
                                <Border Background="#11FF8C00" CornerRadius="8" Padding="20,16" Margin="0,4">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="🔒  This feature is available in ISM+ Pro" FontSize="15" FontWeight="Bold" Foreground="Orange" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                        <TextBlock Text="Unlock real-time network graphs to visualize download and upload speeds over time." FontSize="13" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,12" TextWrapping="Wrap" TextAlignment="Center"/>
                                        <TextBlock HorizontalAlignment="Center" FontSize="13" Foreground="DodgerBlue" TextDecorations="Underline" Cursor="Hand" MouseLeftButtonDown="OpenProStoreLink">
                                            Get ISM+ Pro on the Microsoft Store →
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </GroupBox>

                            <GroupBox FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,15" Padding="10">
                                <GroupBox.Header>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🔒" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                        <TextBlock Text="Floating Desktop Widget" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        <Border Background="#CCFF8C00" CornerRadius="4" Padding="6,2" Margin="10,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="PRO" FontSize="10" FontWeight="Bold" Foreground="White"/>
                                        </Border>
                                    </StackPanel>
                                </GroupBox.Header>
                                <Border Background="#11FF8C00" CornerRadius="8" Padding="20,16" Margin="0,4">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="🔒  This feature is available in ISM+ Pro" FontSize="15" FontWeight="Bold" Foreground="Orange" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                        <TextBlock Text="A fully customisable floating speed widget that sits on your desktop." FontSize="13" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,12" TextWrapping="Wrap" TextAlignment="Center"/>
                                        <TextBlock HorizontalAlignment="Center" FontSize="13" Foreground="DodgerBlue" TextDecorations="Underline" Cursor="Hand" MouseLeftButtonDown="OpenProStoreLink">
                                            Get ISM+ Pro on the Microsoft Store →
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </GroupBox>

                            <GroupBox Header="Taskbar Overlay Speedometer" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,15" Padding="10">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Enable Taskbar Overlay:" Width="180" VerticalAlignment="Center" FontSize="14" FontWeight="Normal"/>
                                        <CheckBox IsChecked="{Binding IsTaskbarOverlayVisible}" VerticalAlignment="Center">
                                            <CheckBox.LayoutTransform><ScaleTransform ScaleX="1.3" ScaleY="1.3"/></CheckBox.LayoutTransform>
                                        </CheckBox>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Hide in FullScreen:" Width="180" VerticalAlignment="Center" FontSize="14" FontWeight="Normal"/>
                                        <CheckBox IsChecked="{Binding HideTaskbarInFullScreen}" VerticalAlignment="Center">
                                            <CheckBox.LayoutTransform><ScaleTransform ScaleX="1.3" ScaleY="1.3"/></CheckBox.LayoutTransform>
                                        </CheckBox>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Lock Position:" Width="180" VerticalAlignment="Center" FontSize="14" FontWeight="Normal"/>
                                        <CheckBox IsChecked="{Binding IsTaskbarLocked}" VerticalAlignment="Center">
                                            <CheckBox.LayoutTransform><ScaleTransform ScaleX="1.3" ScaleY="1.3"/></CheckBox.LayoutTransform>
                                        </CheckBox>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Overlay Opacity:" Width="180" VerticalAlignment="Center" FontSize="14" FontWeight="Normal"/>
                                        <Slider Minimum="0.1" Maximum="1.0" Value="{Binding TaskbarOpacity}" Width="200" VerticalAlignment="Center" TickFrequency="0.1" IsSnapToTickEnabled="True" IsMoveToPointEnabled="True"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Overlay Size:" Width="180" VerticalAlignment="Center" FontSize="14" FontWeight="Normal"/>
                                        <Slider Minimum="0.5" Maximum="3.0" Value="{Binding TaskbarScale}" Width="200" VerticalAlignment="Center" TickFrequency="0.1" IsSnapToTickEnabled="True" IsMoveToPointEnabled="True"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Taskbar Background:" Width="180" VerticalAlignment="Center" FontSize="14" FontWeight="Normal"/>
                                        <ComboBox ItemsSource="{Binding AvailableWidgetColors}" SelectedItem="{Binding SelectedTaskbarBackgroundColor}" Width="150"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Upload Arrow Color:" Width="180" VerticalAlignment="Center" FontSize="14" FontWeight="Normal"/>
                                        <ComboBox ItemsSource="{Binding AvailableWidgetColors}" SelectedItem="{Binding SelectedTaskbarUploadColor}" Width="150"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Download Arrow Color:" Width="180" VerticalAlignment="Center" FontSize="14" FontWeight="Normal"/>
                                        <ComboBox ItemsSource="{Binding AvailableWidgetColors}" SelectedItem="{Binding SelectedTaskbarDownloadColor}" Width="150"/>
                                    </StackPanel>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,15,0,5" Opacity="0.5">
                                        <TextBlock Text="🔒 Flip View Data Period:" Width="180" VerticalAlignment="Center" FontSize="14" FontWeight="Normal" Foreground="Orange"/>
                                        <ComboBox IsEnabled="False" Width="150"/>
                                        <TextBlock Text="(PRO Only)" Margin="10,0,0,0" Foreground="Orange" FontSize="12" VerticalAlignment="Center" FontWeight="Bold"/>
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>

                            <Button Content="Save Settings &amp; Hide" Click="HideToTray_Click" Background="{DynamicResource PrimaryBrush}" Foreground="White" Padding="15,10" Width="200" Margin="0,10,0,20" FontWeight="Bold" HorizontalAlignment="Left" BorderThickness="0" Cursor="Hand"/>
                        </StackPanel>
                    </Border>
                </ScrollViewer>
            </Grid>

        </Grid>
    </Grid>
</Window>
