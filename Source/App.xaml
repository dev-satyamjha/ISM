<Application x:Class="NetMonitor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NetMonitor"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             xmlns:conv="clr-namespace:NetMonitor.Converters"
             Startup="Application_Startup"
             Exit="Application_Exit">
    <Application.Resources>
        <ResourceDictionary>
            <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
            <conv:StringNotEmptyConverter x:Key="StringNotEmptyConverter" />
            
            <Color x:Key="AppPrimaryColor">#FF8C00</Color> 
            <Color x:Key="AppBackgroundColor">#FFFFFF</Color>
            <Color x:Key="AppTextColor">#333333</Color>
            <Color x:Key="AppCardColor">#FFF5E6</Color>
            
            <Color x:Key="UploadSpeedColor">#FF0000</Color>
            <Color x:Key="DownloadSpeedColor">#FFA500</Color>
            <Color x:Key="DownloadUsageColor">#228B22</Color> 
            <Color x:Key="UploadUsageColor">#DAA520</Color> 

            <SolidColorBrush x:Key="PrimaryBrush" Color="{DynamicResource AppPrimaryColor}"/>
            <SolidColorBrush x:Key="BackgroundBrush" Color="{DynamicResource AppBackgroundColor}"/>
            <SolidColorBrush x:Key="TextBrush" Color="{DynamicResource AppTextColor}"/>
            <SolidColorBrush x:Key="CardBrush" Color="{DynamicResource AppCardColor}"/>
            
            <SolidColorBrush x:Key="UploadSpeedBrush" Color="{DynamicResource UploadSpeedColor}"/>
            <SolidColorBrush x:Key="DownloadSpeedBrush" Color="{DynamicResource DownloadSpeedColor}"/>
            <SolidColorBrush x:Key="DownloadUsageBrush" Color="{DynamicResource DownloadUsageColor}"/>
            <SolidColorBrush x:Key="UploadUsageBrush" Color="{DynamicResource UploadUsageColor}"/>
            
            <ContextMenu x:Key="SysTrayMenu">
                <MenuItem Header="Open Dashboard" Command="{Binding OpenDashboardCommand}"/>
                <Separator/>
                <MenuItem Header="Quit" Command="{Binding QuitCommand}"/>
            </ContextMenu>

            
            <Style TargetType="TabItem">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="15,10,15,10"/>
                <Setter Property="Margin" Value="0,0,5,0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Grid>
                                <Border x:Name="Border" 
                                        Background="{TemplateBinding Background}" 
                                        CornerRadius="6" 
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter x:Name="ContentSite"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Center"
                                                      ContentSource="Header"/>
                                </Border>
                                
                                <Border x:Name="Underline" 
                                        Height="3" 
                                        Background="{DynamicResource PrimaryBrush}" 
                                        VerticalAlignment="Bottom" 
                                        Visibility="Collapsed" 
                                        CornerRadius="1.5"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Underline" Property="Visibility" Value="Visible"/>
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsSelected" Value="False"/>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource CardBrush}"/>
                                    <Setter TargetName="Border" Property="Opacity" Value="0.8"/>
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            
            <Style TargetType="TabControl">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0,10,0,0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabControl">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <Border Grid.Row="0" Margin="0,0,0,10">
                                    <TabPanel Margin="5,0,0,0" IsItemsHost="True" Background="Transparent" KeyboardNavigation.TabIndex="1"/>
                                </Border>
                                
                                <Border Grid.Row="1" Background="{TemplateBinding Background}" BorderThickness="0">
                                    <ContentPresenter ContentSource="SelectedContent"/>
                                </Border>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
